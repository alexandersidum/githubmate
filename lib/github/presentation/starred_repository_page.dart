import 'package:flutter/material.dart';

class StarredRepository extends StatelessWidget {
  const StarredRepository({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
